# Setup Guide — Backend

> Пошаговая инструкция по настройке окружения для бэкенда «Омут памяти».
> Этот документ — для себя в будущем и для коллег, которые будут разворачивать проект.

---

## 0. Требования

- **Python 3.11+** ([python.org/downloads](https://www.python.org/downloads/))
- **Docker Desktop** ([docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop/)) — для PostgreSQL
- **Git** ([git-scm.com](https://git-scm.com/))

Проверка установки:
```bash
python --versions
docker --version
git --version